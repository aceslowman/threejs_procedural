import { connect } from 'react-redux';
//import actions
import TerrainGUI from '../components/gui/TerrainGUI';

const mapStateToProps = (state, ownProps) => ({

});

const mapDispatchToProps = (dispatch, ownProps) => ({

});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(TerrainGUI);
