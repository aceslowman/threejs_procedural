import * as THREE from 'three';
import RoadSystem from './RoadSystem';

export default class RoadMesh{
  constructor(world,roadSystem){
    this.world = world;
    this.roadSystem = roadSystem;
  }
}
